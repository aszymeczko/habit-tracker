const initialState = {
  data: [], // Przechowywane dane z API
  loading: false, // Czy dane są w trakcie ładowania
  error: null, // Obsługa błędów
  searchQuery: "", // nowy stan wyszukiwania
};

export default initialState;
